# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Génération vidéo ---
# torch n'est PAS épinglé ici : installe la build adaptée à TON GPU AVANT
# d'installer ce fichier (voir README.md) :
#   NVIDIA (CUDA) : pip install torch --index-url https://download.pytorch.org/whl/cu124
#   AMD (ROCm)    : pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
diffusers>=0.32.0
transformers>=4.44
accelerate>=0.33
safetensors>=0.4
sentencepiece>=0.2
imageio>=2.34
imageio-ffmpeg>=0.5
pillow>=10.0
